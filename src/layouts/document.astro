---
import Header from "../components/header.astro";
import Footer from "../components/footer.astro";
import "../styles/global.scss";
import Sidebar from "../components/sidebar.astro";
---

<html>
  <head>
    <title>RIP Wallet</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta charset="utf-8" />

    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#79e091" />
    <meta name="msapplication-TileColor" content="#79e091" />
    <meta name="theme-color" content="#79e091" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Work+Sans:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
      rel="stylesheet"
    />
    <slot name="head" />
  </head>

  <body>
    <Sidebar sidebarId="sidebar" closeId="sidebarClose" />
    <Header buttonId="sidebarToggle" />

    <div class="contentarea">
      <slot />
    </div>

    <Footer />
  </body>
</html>

<script>
  /* Mobile sidebar */
  let expanded = false;

  const sidebarToggleEl = document.querySelector("#sidebarToggle");
  const sidebarEl = document.querySelector("#sidebar") as HTMLElement;
  const sidebarCloseEl = document.querySelector("#sidebarClose") as HTMLElement;

  function toggle() {
    if (expanded) {
      sidebarEl.style.left = "-200vw";
    } else {
      sidebarEl.style.left = "0px";
    }
    expanded = !expanded;
  }

  sidebarToggleEl.addEventListener("click", toggle);

  sidebarCloseEl.addEventListener("click", toggle);
</script>

<style lang="scss">
  .contentarea {
    min-height: 1px;
    flex-grow: 1;
    width: 100%;
    box-sizing: border-box;
    padding: 24px;
  }
</style>
